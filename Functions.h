#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>

using namespace std;


string getAllText();							

void startAlgorithm();						

string removeWords(string);			

string eliminateWords(string, string);			

string getSentenceFromTruman(string, string);		

bool isWanted(string, string);				

string clearSentence(string);					
